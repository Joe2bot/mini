
## specification 阶段
先不要写代码。阅读 AGENTS.md 和 specs/ 下所有 Spec，理解整个 Mini Coze V1 的架构和目标。分析模块依赖、开发顺序以及可能存在的 Spec 冲突或风险。输出你的理解和实施计划，等待我的确认。

## Design 阶段
Spec 已确认。现在进入 Design 阶段。请不要编写业务代码。基于当前全部 Spec，为 Mini Coze V1 输出详细的技术设计方案。重点明确：1）Spring Boot 与 Python Runtime 的 HTTP/SSE 通信；2）数据库实体与表结构；3）Agent/Workflow/Run/Trace 的数据模型；4）统一 State Schema；5）Tool Registry；6）LangGraph Workflow 映射；7）RAG Pipeline；8）MCP Client 与 Tool 映射；9）SSE Event Schema；10）项目目录结构；11）模块依赖关系；12）错误处理与超时机制。请指出设计中需要我确认的决策，但不要修改业务代码。

## Design 已审阅
Design 已审阅，通过。请根据以下最终决策更新 DESIGN.md，不修改业务代码：
原始文件使用 Docker Volume，不引入 S3/MinIO。
Secret 仅通过环境变量及 environment reference 管理，V1 不引入 Secret Manager。
Runtime V1 不做任务持久化恢复；Runtime 重启导致中的 Run 标记为 failed。
Workflow Condition 使用受限结构化规则：equals、not_equals、exists、truthy，不支持脚本或通用表达式。
LLM 和 Embedding 使用 Adapter 抽象，通过环境变量配置 Provider 和 Model，不将具体 Provider 硬编码到业务架构。
保持当前 Spring Boot / Python Runtime / PostgreSQL / LangGraph / LangChain / Qdrant / MCP 的架构不变。
更新后检查 Spec 与 Design 是否一致。
不开始业务代码实现。
完成后输出最终 Design 变更摘要，并等待确认。


## 先把最小闭环跑通，然后再加 Tool Calling → Workflow → RAG → MCP → Trace。

